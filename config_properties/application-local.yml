# Served by config server when client runs with profile "local" (e.g. IDE run).
# Use localhost:9093 (Kafka EXTERNAL listener) so broker advertises localhost and connection stays stable.
spring:
  kafka:
    bootstrap-servers: localhost:9093
